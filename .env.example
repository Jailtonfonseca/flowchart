# Server configuration
SERVER_SECRET_KEY=change_this_to_a_secure_random_string_in_production
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Optional OpenRouter Key (can also be provided via UI)
# OPENROUTER_API_KEY=sk-or-...

# Optional: Set to true to disable encryption for testing (if implemented)
# DEV_MODE=true
